/**
 * Function that returns the supplied value. Useful for certain compositions
 * and a key components for many functional algebraic laws.
 * 
 * @param {any} x
 */
export const identity = x => x;
export { identity as id };
